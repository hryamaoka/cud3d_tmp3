# `src/utils/utils.c` 詳細解説

## 概要
プロジェクト全体で使用される汎用的な補助関数を提供します。現在は絶対値計算関数が含まれています。

## コード詳細解説

| 行番号 | コード | 解説 |
| :--- | :--- | :--- |
| **15** | `int ft_abs(int n)` | **絶対値関数**<br>整数`n`の絶対値を返します。負の値であれば符号を反転させ、非負であればそのまま返します。スプライトサイズ計算時の負の値補正などで使用されます。 |

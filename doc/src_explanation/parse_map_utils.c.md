# `src/parser/parse_map_utils.c` 詳細解説

## 概要
マップパース時のメモリ管理と文字列整形ヘルパー関数です。

## コード詳細解説

| 行番号 | コード | 解説 |
| :--- | :--- | :--- |
| **17** | `char **change_line_to_map(...)` | **配列リサイズ**<br>古いマップ配列（サイズN）の内容を新しい配列（サイズN+1）にコピーし、新しい行を追加します。古いコンテナ（ポインタ配列）のみ解放します。 |
| **36** | `void cleanup_line(char *line)` | **改行削除**<br>文字列末尾の`\n`や`\r`を`\0`に置換して削除します。 |
